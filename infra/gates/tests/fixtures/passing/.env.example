# Local runs only (.env is gitignored). In the playground these come from app.yaml `env:`.
LOG_LEVEL=info
# Pretend to be a signed-in user locally. In the playground the platform sets the real
# identity headers before any request reaches the code; you never set these yourself.
LOCAL_USER_EMAIL=you@example.org
LOCAL_USER_NAME=Local Developer
